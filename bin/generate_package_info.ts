const packageJson = require('../package.json');

export const version = JSON.stringify(packageInfo.version);
export const homepage = JSON.stringify(packageInfo.homepage);
export const author = JSON.stringify(packageInfo.author.name);
